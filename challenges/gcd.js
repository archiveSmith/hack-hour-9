/**
 * Given two integers, return the greatest common divisor.
 *
 * Example:
 * gcd(10, 8) -> 2
 * gcd(10, 9) -> 1
 *
 */

function gcd(a, b) {
return b ? gcd(b, a % b) : Math.abs(a);
}

module.exports = gcd;